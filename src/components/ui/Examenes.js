import React from 'react'

export const Examenes = ({id,nombreExamen}) => {
    return (
        <div>
            <span>{id}</span>
            <span>{nombreExamen}</span>
            
        </div>
    )
}
